{
  "name": "xunmeng-dream-agent",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "python -m compileall backend/app backend/tests && npm --prefix frontend run build",
    "test:backend": "python -m unittest discover -s backend/tests -v",
    "qa:model": "python scripts/model_qa.py"
  }
}
